<%= form_for(equipment_status) do |f| %>
  <% if equipment_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_status.errors.count, "error") %> prohibited this equipment_status from being saved:</h2>

      <ul>
      <% equipment_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :desc %>
    <%= f.text_field :desc %>
  </div>

    <div class="field">
      <%= f.label :group_id, '状态分组' %>
      <%= f.select(:group_id, EquipmentStatusGroupEnum.menu, {include_blank: false}) %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
