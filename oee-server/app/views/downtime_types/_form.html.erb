<%= form_for(downtime_type) do |f| %>
  <% if downtime_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(downtime_type.errors.count, "error") %> prohibited this downtime_type from being saved:</h2>

      <ul>
      <% downtime_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nr %>
    <%= f.text_field :nr %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
