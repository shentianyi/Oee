<%= form_for(equipment_release) do |f| %>
  <% if equipment_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_release.errors.count, "error") %> prohibited this equipment_release from being saved:</h2>

      <ul>
      <% equipment_release.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :equipment_track_id %>
    <%= f.text_field :equipment_track_id %>
  </div>

  <div class="field">
    <%= f.label :release_index %>
    <%= f.number_field :release_index %>
  </div>

  <div class="field">
    <%= f.label :release_time %>
    <%= f.text_field :release_time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
